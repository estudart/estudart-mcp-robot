services:
  robot-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: robot-mcp-server
    restart: unless-stopped
    ports: 
      - "8000:8000"
    devices:
      - "/dev/i2c-1:/dev/i2c-1"
